ポインタを実装する最初のステップとして、アドレスを返す単項＆と、アドレスを参照する単項*を実装する。

これらの演算子は本来はポインタ型の値を返したり、ポインタ型の値をとったりする演算子ですが、現状のコンパイラには整数以外の型がない。
ポインタ型は整数型で代用する。

すなわち、&xは変数xのアドレスを単なる整数として返す、また*xはxの値をアドレスとみなして、そのアドレスから値を読んでくるという演算をする。

動かしたいコード
```
x = 3;
y = &x;
return *y;  // 3を返す
```


また、ローカル変数がメモリ上に連続して割り当てられているということを利用して、スタック上の変数にポインタ型ゆで間接的に無理やりアクセスすることもできるようになる。

```
x = 3;
y = 5;
z = &y + 8;
return *z; // 3を返す
```

実装は比較的簡単らしい。

単項&と単項*を追加した文法は、
```
unary = "+"? primary
      | "-"? primary
      | "*" unary
      | "&" unary
```
		
となるらしい。


今の実装では、*p = 99;
といったデリファレンスされた場所への代入が正しくコンパイルできない。

その理由すら分からない。調査

ちなみに、この「演算子の優先順位を関数の呼び出し階層で表現する」手法は、Pratt parser(1973)のアイデアが元になってリウ。
Vaughan Prattが発表した当時はほとんど注目されていなかったが、2000年代にDouglas Crockford(JSONの生みの親)がJavascriptパーサーで採用して、再発見され、今では多くのコンパイラで使われている。

*p = 88;

これをコンパイルでいるようになった理由。




