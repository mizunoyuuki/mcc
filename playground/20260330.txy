### 豆知識

「後置演算子」の統一的な処理は、プロダクションコンパイラの定石
[], (), .(メンバアクセス), -> はいずれも　「式の後ろにくる後置演算子(postfix operator)」です。
C言語の規格ではこれらを「postfix expression」とまとめて定義されている。


++i, i++,
も、前置演算子と、後置演算子の違いであり、

ちなみに、i++は、先にiの値を待避させておいてからインクリメントするため、追加のコピーを必要とする。
そのため、基本的に後置の単項インクリメントが推奨
